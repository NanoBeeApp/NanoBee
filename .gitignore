node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json

# Local secrets (public repository — never commit credentials)
.dev.vars
!.dev.vars.example
!.env.example
*.gen.ts
tanstack-start-demo

# Localflare generated files
.localflare/
.playwright-mcp

# Dev logs
logs/*.log
/*.png
build
.build

# Local Claude Code settings (machine-specific, not part of the public repo)
.claude/settings.local.json

# Private content (independent private repo NanoBee_Private — never commit to the public main repo)
/private/

# agy local output buffer (temporary, never committed)
.agy-output/

# TypeScript incremental build artifact
tsconfig.tsbuildinfo
