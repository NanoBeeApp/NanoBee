# Local development (loaded automatically by `vite dev`).
# All VITE_* variables are baked into the JS bundle at build time and are
# readable from both the browser and SSR — never put secrets here.
# Server-only variables belong in wrangler.json env.*.vars (non-secret)
# or `wrangler secret` (secret).
