# src/components/chat/RecommendedActions.tsx

## Responsibility
"建议的操作" list card inside an AI reply (tone badge + label + CTA per row).

## Dependencies
- Upstream: types (RecActionItem)
- Downstream: MessageView (extra kind 'rec')

## Change history

### 2026-06-12 — created
- **Motivation**: ported from the prototype to keep the message-extra vocabulary complete (not used by seeded demo data yet).
